[project]
name = "stock-predictor"
version = "0.1.0"
description = ""
authors = [
    {name = "acabellom",email = "acabellom01@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
]

[tool.poetry]
packages = [{include = "stock_predictor", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
